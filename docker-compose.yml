services:
  leo-routing-simu:
    build: .
    image: ghcr.io/fundacio-i2cat/leopath:latest
    container_name: leo-routing-simu
    volumes:
      - ./leopath/config:/app/leopath/config
      - ./output/logs:/app/logs
      - ./output/tles:/app/tle_output
      - ./output/simulation:/app/simulation_output
      - ./output/visualizations:/app/visualisation_output
      - .:/app/output:delegated
    working_dir: /app/output
    environment:
      - PYTHONPATH=/app
        
  # Service for visualization
  leo-routing-viz:
    build: .
    image: ghcr.io/fundacio-i2cat/leopath:latest
    container_name: leo-routing-viz
    volumes:
      - ./leopath/config:/app/leopath/config
      - ./output/logs:/app/logs
      - ./output/tles:/app/tle_output
      - ./output/simulation:/app/simulation_output
      - ./leopath/satellite_visualisation/visualisation_output:/app/leopath/satellite_visualisation/visualisation_output
      - .:/app/output:delegated
    working_dir: /app/output
    environment:
      - PYTHONPATH=/app
    entrypoint: ["python", "-m", "leopath.satellite_visualisation.cesium_builder.main"]
    
  # HTTP server for viewing visualizations
  viz-server:
    image: nginx:alpine
    container_name: leo-routing-viz-server
    ports:
      - "8080:80"
    volumes:
      - ./leopath/satellite_visualisation/cesium_builder/visualisation_output:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - leo-routing-viz
